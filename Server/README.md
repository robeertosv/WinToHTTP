El servidor está configurado por defecto en el puerto 3000, requiere instalar Express, opcionalmente puede usar nodemon para hacer hot reloads

Usar con:

```bash
npm install
npm start
```
O
```bash
npm install
npm run dev
```
